# coding: utf-8

"""
    Forza Open API

    API REST ouverte, gratuite et communautaire pour les données Forza Horizon (FH6 d'abord, FH5 en backfill). Contract-first : ce fichier est la source de vérité ; le serveur Go (ogen) et les clients TS en découlent. Champ `game` partout (game-agnostic). Erreurs en RFC 9457. 

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from forza_open_api_client.models.meta_game import MetaGame
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class Meta(BaseModel):
    """
    Métadonnées du service : jeux supportés, volumes et fraîcheur des données. Pensé pour les consommateurs (sélecteur de jeu, indicateur « data à jour ? ») et le dogfooding. games liste un MetaGame par valeur de l'enum Game (les jeux sans données apparaissent à 0). 
    """ # noqa: E501
    games: List[MetaGame] = Field(description="Jeux supportés et leurs volumes / fraîcheur (un par valeur de l'enum Game).")
    data_version: Optional[StrictStr] = Field(default=None, description="Version du jeu de données publiée (tampon opérateur, ex. snapshot wiki daté). Absente si non renseignée. ", alias="dataVersion")
    generated_at: datetime = Field(description="Instant de calcul de la réponse (permet d'estimer l'âge des données côté client).", alias="generatedAt")
    __properties: ClassVar[List[str]] = ["games", "dataVersion", "generatedAt"]

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of Meta from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in games (list)
        _items = []
        if self.games:
            for _item_games in self.games:
                if _item_games:
                    _items.append(_item_games.to_dict())
            _dict['games'] = _items
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of Meta from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "games": [MetaGame.from_dict(_item) for _item in obj["games"]] if obj.get("games") is not None else None,
            "dataVersion": obj.get("dataVersion"),
            "generatedAt": obj.get("generatedAt")
        })
        return _obj


